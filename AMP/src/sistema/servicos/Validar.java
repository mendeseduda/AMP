/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package sistema.servicos;

import sistema.model.Comissao;
import sistema.model.Grupo;
import sistema.model.Maratona;
import sistema.model.Organizador;
import sistema.model.Tutor;

/**
 *
 * @author aluno
 */
public class Validar {
    /**
     * Metodo que verifica a validade do cadastro de um grupo no sistema.
     * @param grupo Objeto o qual se verifica a validade de cadastro no sistema. 
     * @return Retorna a validade (verdadeiro ou falso)
     */
    public boolean validarCadastro(Grupo grupo){
        boolean validade = false;//retorno do metodo, que retorna true caso seja valido o cadastro, ou retorna false 
        if(/*for valido*/true){
            return true;
        }
        return false;
    }
    /**
     * Metodo que verifica a validade do cadastro de um comissario no sistema.
     * @param comissao Objeto o qual se verifica a validade de cadastro no sistema. 
     * @return Retorna a validade (verdadeiro ou falso)
     */
    public boolean validarCadastro(Comissao comissao){
        boolean validade = false;//retorno do metodo, que retorna true caso seja valido o cadastro, ou retorna false 
        if(/*for valido*/true){
            return true;
        }
        return false;
    }
    /**
     * Metodo que verifica a validade do cadastro de um tutor no sistema.
     * @param tutor Objeto o qual se verifica a validade de cadastro no sistema. 
     * @return Retorna a validade (verdadeiro ou falso)
     */
    public boolean validarCadastro(Tutor tutor){
        boolean validade = false;//retorno do metodo, que retorna true caso seja valido o cadastro, ou retorna false 
        if(/*for valido*/true){
            return true;
        }
        return false;
    }
    /**
     * Metodo que verifica a validade do cadastro de um organizador no sistema.
     * @param organizador Objeto o qual se verifica a validade de cadastro no sistema. 
     * @return Retorna a validade (verdadeiro ou falso)
     */
    public boolean validarCadastro(Organizador organizador){
        boolean validade = false;//retorno do metodo, que retorna true caso seja valido o cadastro, ou retorna false 
        if(/*for valido*/true){
            return true;
        }
        return false;
    }
    /**
     * Metodo que verifica a validade do cadastro de uma maratona no sistema.
     * @param maratona Objeto o qual se verifica a validade de cadastro no sistema. 
     * @return Retorna a validade (verdadeiro ou falso)
     */
    public boolean validarCadastro(Maratona maratona ){
        boolean validade = false;//retorno do metodo, que retorna true caso seja valido o cadastro, ou retorna false 
        if(/*for valido*/true){
            return true;
        }
        return false;
    }
    
}
